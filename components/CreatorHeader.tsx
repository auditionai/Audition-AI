
// Re-export the actual component to maintain compatibility while fixing build errors
// caused by the file being in the wrong directory with incorrect relative imports.
export { default } from './creator/CreatorHeader';
